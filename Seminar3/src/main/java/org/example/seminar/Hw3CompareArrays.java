package org.example.seminar;

import java.util.ArrayList;

public class Hw3CompareArrays{
    public static <T> boolean compareArrays(ArrayList<T> first, ArrayList<T> second){
        return first.equals(second);
    }
}
